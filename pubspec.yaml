name: reactive_notifier
description: A Dart library for managing application state reactively and efficiently. Implements MVVM-inspired ViewModels and Notifiers for fine-grained control over complex, interconnected states and UI updates.

version: 2.10.2
homepage: https://github.com/JhonaCodes/reactive_notifier.git

environment:
  sdk: ^3.5.4
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  lints: ^4.0.0
  test: ^1.24.0
  alchemist: ^0.12.1

flutter:
  uses-material-design: true
